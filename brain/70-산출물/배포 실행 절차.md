---
type: 산출물
status: 진행중
tags: [fitter, 배포, phase5]
updated: 2026-07-26
---

# 배포 실행 절차

> 준비 상태와 판단은 [[배포 준비 상태]]에 있다. **여기는 손으로 무엇을 누르는가**다.
> 도메인은 `xvsadfadsfasdfsdafsda.uk` (Cloudflare Registrar, 연 $5.30).
>
> **실제로 따라가면서 걸린 것들은 [[배포에서 막힌 것들]]에 있다.** 배포는 여섯 번
> 실패했고 원인이 전부 로컬에서 재현되지 않는 종류였다. 다음 배포 전에 그걸 먼저 읽어라.

## 어디에 무엇이 가나

| 조각 | 어디 | 주소 |
| --- | --- | --- |
| 웹 (Next.js) | Vercel Hobby (무료) | `xvsadfadsfasdfsdafsda.uk` |
| API (NestJS) | Railway (월 $5) | `api.xvsadfadsfasdfsdafsda.uk` |
| PostgreSQL | Railway 애드온 | 내부 네트워크 |
| Redis | Railway 애드온 | 내부 네트워크 |
| 이미지 | Cloudflare R2 (무료) | `img.xvsadfadsfasdfsdafsda.uk` |

## Railway — 레포 연결만으로는 안 된다

**이게 이 배포에서 가장 헷갈리는 지점이다.** pnpm 워크스페이스 모노레포라
Railway 의 자동 감지가 무엇을 빌드할지 모른다. 레포 루트에 `apps/web` 과 `apps/api`
가 같이 있고, `packages/*` 를 **먼저 순서대로** 빌드해야 api 가 빌드된다.

그래서 `railway.json` 을 레포 루트에 두고 명령을 직접 적었다.

### `railway.json` 이 하는 일

```
buildCommand   prisma generate 후 shared → domain → api 순서로 빌드
preDeployCommand  prisma migrate deploy — 새 버전이 살아나기 전에 스키마를 맞춘다
startCommand   node apps/api/dist/main.js
healthcheckPath  /api/health
```

**`--filter "@fitter/api..."` 의 점 셋이 핵심이다.** 이게 "그 패키지와 그것이 의존하는
것들"을 뜻해서 `shared → domain → api` 를 위상 순서로 고르고 **웹은 빼놓는다.**
로컬에서 실제로 확인했다. `pnpm -r build` 로 하면 쓰지도 않을 웹까지 빌드한다.

**`prisma generate` 가 빌드보다 먼저여야 한다.** `PrismaService` 가 생성된 클라이언트를
상속하므로 없으면 타입이 없어 빌드가 깨진다. 로컬은 이미 생성돼 있어서 이 순서를
빠뜨려도 통과하는데 **빈 컨테이너에서만 터진다** — 로컬에서 재현되지 않는 종류다.

**마이그레이션을 시작 명령이 아니라 pre-deploy 로 뒀다.** 시작 명령에 넣으면 컨테이너가
재시작될 때마다 돌고, 마이그레이션이 실패하면 부팅 자체가 크래시 루프가 된다.

> [!warning] `preDeployCommand` 가 안 먹으면
> Railway 스키마가 이 필드를 거부하면 대시보드의 **Pre-Deploy Command** 칸에 같은
> 명령을 넣는다. 그것도 없으면 최초 1회만 Railway CLI 로 직접 돌린다.
> `prisma` 는 devDependency 라 Railway 가 prod 프루닝을 하면 사라질 수 있다 —
> 그때는 `apps/api/package.json` 에서 `prisma` 를 dependencies 로 옮긴다.

### 대시보드에서 할 것

1. 프로젝트 생성
2. **`+ New` → Database → PostgreSQL** 추가 (설정할 것 없다)
3. **`+ New` → Database → Redis** 추가
4. **`+ New` → GitHub Repo → `rhdqngusanr/fitter`** 선택
   - **Root Directory 는 비워둔다.** `apps/api` 로 잡으면 워크스페이스 패키지가
     레포 루트에 있어서 설치가 깨진다
   - 빌드·시작 명령은 `railway.json` 이 이미 준다
5. api 서비스 → Settings → Networking → **Custom Domain** 에 `api.xvsadfadsfasdfsdafsda.uk`
6. Variables 탭에서 아래 환경변수를 넣는다

### Railway 환경변수

DB·Redis 는 **참조 변수**로 넣는다. 값을 복사하면 Railway 가 비밀번호를 회전할 때 끊긴다.

| 변수 | 값 |
| --- | --- |
| `NODE_ENV` | `production` |
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}` |
| `REDIS_URL` | `${{Redis.REDIS_URL}}` |
| `WEB_ORIGIN` | `https://xvsadfadsfasdfsdafsda.uk` |
| `JWT_SECRET` | 직접 생성. **로컬 것을 재사용하지 마라** |
| `STORAGE_BUCKET` | `fitter-images` |
| `STORAGE_ENDPOINT` | R2 의 S3 API 엔드포인트 |
| `STORAGE_ACCESS_KEY_ID` | R2 토큰 |
| `STORAGE_SECRET_ACCESS_KEY` | R2 토큰 |
| `STORAGE_REGION` | `auto` |
| `SENTRY_DSN` | Sentry 프로젝트(Node) |

`PORT` 는 넣지 않는다. Railway 가 주입하고 `config/env.ts` 가 그걸 읽는다.

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

> [!danger] `WEB_ORIGIN` 이 틀리면 화면이 조용히 죽는다
> `credentials: true` 라 CORS 가 **정확한 오리진 일치**를 요구한다. 틀리면 SSR 은 되는데
> 클라이언트 fetch 만 전부 막혀서 "화면은 그려지는데 버튼이 안 먹는다". 로컬에서
> 이미 한 번 겪었다. 끝에 슬래시를 붙이지 마라.

## Vercel — 웹

- **Root Directory**: `apps/web`
- **Build Command**: `cd ../.. && pnpm --filter "@fitter/web..." build`
  기본값(`next build`)으로 두면 **`@fitter/shared` 가 빌드되지 않아 깨진다** —
  `main` 이 `dist/index.js` 를 가리키는데 그 파일이 없다
- **Install Command**: 기본값 유지. Vercel 이 워크스페이스를 알아서 루트에서 설치한다
- Domains 에 `xvsadfadsfasdfsdafsda.uk` 연결

### Vercel 환경변수

| 변수 | 값 |
| --- | --- |
| `NEXT_PUBLIC_API_URL` | `https://api.xvsadfadsfasdfsdafsda.uk/api` |
| `NEXT_PUBLIC_IMAGE_BASE_URL` | `https://img.xvsadfadsfasdfsdafsda.uk` |
| `NEXT_PUBLIC_SENTRY_DSN` | Sentry 프로젝트(Next.js) |
| `NEXT_PUBLIC_ALLOW_INDEXING` | **넣지 않는다.** 없으면 전면 noindex 다 |
| `SENTRY_ORG` `SENTRY_PROJECT` `SENTRY_AUTH_TOKEN` | 선택. 소스맵 업로드용 |

`NEXT_PUBLIC_API_URL` 끝의 `/api` 를 빠뜨리지 마라. Nest 가 전역 프리픽스로 `api` 를 쓴다.

## R2 · DNS

버킷 `fitter-images` → Settings → **Public access 를 커스텀 도메인 `img.xvsadfadsfasdfsdafsda.uk` 로** 연다.
`r2.dev` 는 Cloudflare 가 속도를 제한하므로 쓰지 않는다. 토큰은 **Object Read & Write**.

**공개 읽기가 열려야 사진이 보인다.** 목록·상세의 썸네일은 서명 URL 이 아니라
`imageUrl(key)` 가 만든 평범한 `<img>` 주소다. 업로드는 서명 URL 이라 쓰기는 열지 않는다.

DNS 는 Cloudflare 한 곳에서 관리한다 — 도메인을 Cloudflare Registrar 에서 샀으므로
네임서버를 옮길 필요가 없다.

## 배포 후 순서

1. `pnpm --filter @fitter/api db:seed` — **공종 13종·지역.** 이게 없으면 의뢰 등록 폼이 빈다
2. `pnpm --filter @fitter/api db:seed:admin <email> <password>` — 로컬 비밀번호를 쓰지 마라
3. `db:seed:demo` 는 **선택.** 넣으면 콜드스타트 화면을 안 보고, 안 넣으면 진짜 첫인상을 본다
4. `/api/health` 200 확인
5. 로그인 → **새로고침** → 로그인이 유지되는지 확인 (쿠키가 제대로 붙었는지 보는 유일한 방법)
6. `/qc-screens` 로 배포본 화면 검사

## 친구들에게 열기 전에 남은 것

**rate limit.** [[배포 준비 상태]] 참고. 신고는 비로그인도 낼 수 있고 인정되면 대상이
즉시 비공개가 된다.

## 연결

[[배포 준비 상태]] · [[P5 - 배포와 문서화]] · [[진행 현황판]] · [[기술 스택 결정]] ·
[[화면 QC 목록]]
