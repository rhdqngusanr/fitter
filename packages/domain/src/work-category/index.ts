/**
 * 공종 — 코드 테이블. 문자열이 아니다.
 *
 * 여기에 들어올 것 (P3-1에서 스키마 확정, P4에서 구현):
 * - work-category.ts  code / name_ko / parent_id / sort_order / is_active
 *
 * 확장 규약 2조. 문자열로 두면 "도배", "도배요", "도배 및 장판"이 전부 다른 값이 되어
 * 필터도 통계도 단가도 불가능해진다.
 *
 * 시안 검수에서 화면마다 공종 목록이 다섯 갈래로 갈라져 있는 것이 발견됐다.
 * 코드 테이블이었다면 애초에 불가능한 일이다. 정본은 시드 13종이고 모든 화면이 이것만 참조한다.
 *
 * parent_id 로 계층을 지원한다. 초기엔 1단계로 시작해도 나중에 "도배" 아래
 * "실크 도배 / 합지 도배"가 생길 때 테이블을 안 바꿔도 되게.
 *
 * 근거: brain/20-도메인/엔티티 - WorkCategory.md · brain/30-설계/시안 검수 결과.md 4번
 */
export {};
