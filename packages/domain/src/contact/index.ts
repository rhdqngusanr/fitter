/**
 * 컨택 — 양방향 매칭 요청. **기술 하이라이트 2번.**
 *
 * 여기에 들어올 것 (P4-6에서 구현):
 * - contact-status.ts   REQUESTED / ACCEPTED / DECLINED / CANCELLED / EXPIRED
 * - transitions.ts      현재 상태·행위자·원하는 전이를 받아 새 상태 또는 도메인 에러를 반환하는 순수 함수
 * - events.ts           전이가 발행하는 도메인 이벤트
 *
 * 이 모듈의 규칙은 두 줄로 요약된다.
 *   1. 상태만 맞아서는 안 되고 **주체까지 맞아야 한다.** 수락은 수신자만, 취소는 요청자만.
 *   2. 연락처는 ACCEPTED 이후에만 응답에 실린다. 그 통제는 여기가 아니라
 *      apps/api 의 응답 직렬화 레이어 한 곳에서 강제한다.
 *
 * 전이 함수를 순수 함수로 두는 이유는 DB 없이 테스트하기 위해서다.
 * 유효·무효 조합 최소 15케이스가 P4-6의 완료 조건이다.
 *
 * 근거: brain/20-도메인/상태머신 - 컨택.md · brain/30-설계/권한 모델.md
 */
export {};
