/**
 * 컨택 — 양방향 매칭 요청. **기술 하이라이트 2번.**
 *
 * 이 모듈의 규칙은 두 줄로 요약된다.
 *   1. 상태만 맞아서는 안 되고 **주체까지 맞아야 한다.** 수락은 수신자만, 취소는 요청자만.
 *   2. 연락처는 ACCEPTED이고 당사자일 때만 공개된다. 그 판정은 canRevealContact 하나가 한다.
 *
 * 전이 함수를 순수 함수로 둔 이유는 DB 없이 전 조합을 테스트하기 위해서고,
 * 이 판단을 DB 트리거로 옮길 수 없는 이유이기도 하다 —
 * DB는 누가 이 UPDATE를 호출했는지 모른다.
 *
 * 근거: brain/20-도메인/상태머신 - 컨택.md
 */

export * from './transitions';
