/**
 * 이미지 — 사진이 제품의 전부인 서비스라 여기가 곧 품질이다.
 *
 * 규칙을 도메인에 두는 이유는 클라이언트와 서버가 **같은 규칙**을 봐야 하기 때문이다.
 * 두 곳에 따로 쓰면 반드시 어긋나고, 그때 사용자는 "올렸는데 실패했다"를 겪는다.
 *
 * 근거: brain/20-도메인/이미지 파이프라인.md
 */

export * from './image-rules';
export * from './magic-number';
