/**
 * 응답 직렬화 — **이 프로젝트에서 가장 위험한 지점.**
 *
 * 상대의 연락처는 컨택이 ACCEPTED일 때만 응답에 포함된다.
 * 그런데 이걸 컨트롤러마다 if문으로 막으면 반드시 어딘가 빠진다.
 * 컨택 상세, 컨택 목록, 의뢰 상세에 딸린 요청자 정보, 시공자 프로필 — 경로가 넷이다.
 *
 * 그래서 **응답이 나가는 마지막 지점 한 곳에서 막는다.**
 * 기본값은 "포함하지 않음"이고, 명시적으로 공개 조건을 만족할 때만 채운다.
 * 새 엔드포인트를 추가해도 별도 조치 없이 안전한 쪽이 기본이어야 한다.
 *
 * 그리고 화면에서 안 보이는 것과 응답에 없는 것은 완전히 다르다.
 * 마스킹된 문자열을 넣는 게 아니라 **키 자체가 없어야 한다.**
 * 이걸 검증하는 테스트를 네 경로 각각에 쓴다. (유저 스토리 US-042)
 *
 * 여기 들어올 것 (P4-6):
 *   contact-privacy.interceptor.ts
 *
 * 근거: brain/30-설계/권한 모델.md · brain/20-도메인/상태머신 - 컨택.md
 */
export {};
