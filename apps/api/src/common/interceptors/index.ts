/**
 * 응답 직렬화 — **이 프로젝트에서 가장 위험한 지점.**
 *
 * 상대의 연락처는 컨택이 ACCEPTED일 때만 응답에 포함된다.
 * 컨트롤러마다 if문으로 막으면 반드시 어딘가 빠진다 —
 * 컨택 상세, 컨택 목록, 의뢰 상세에 딸린 요청자 정보, 시공자 프로필. 경로가 넷이다.
 *
 * 그래서 응답이 나가는 마지막 지점 한 곳에서 막는다.
 * 기본값은 "제거"이고 명시적으로 공개할 때만 채운다.
 *
 * 근거: brain/30-설계/권한 모델.md · brain/20-도메인/상태머신 - 컨택.md
 */

export * from './contact-privacy.interceptor';
