/**
 * 인프라 어댑터 — 포트의 구현체가 사는 곳.
 *
 * 도메인이 정의한 인터페이스(@fitter/domain 의 ports/)를 여기서 실제 기술로 채운다.
 * **의존 방향은 항상 안쪽이다.** 도메인은 이 폴더를 모른다.
 *
 * 앞으로 들어올 것:
 *   prisma/   저장소 구현. Prisma를 여기 가둬서 나중에 다른 ORM으로 바꿔도
 *             도메인과 서비스가 흔들리지 않게 한다. 스키마는 P3-1 ERD 이후.
 *   storage/  StoragePort 구현. Cloudflare R2(S3 호환) presigned URL. P4-2.
 *   queue/    BullMQ. 썸네일 파생과 고아 파일 정리. P4-2.
 *   events/   EventBusPort 구현. 인메모리로 시작해 나중에 큐로 바꾼다. P4-6.
 *
 * 각 어댑터의 의존성은 그 Phase에서 추가한다. 지금 미리 설치하지 않는 이유는
 * 쓰지 않는 라이브러리가 스캐폴딩에 섞이면 무엇이 실제로 필요한지 알 수 없게 되기 때문이다.
 *
 * 근거: brain/30-설계/구조적 원칙.md 2조
 */
export {};
